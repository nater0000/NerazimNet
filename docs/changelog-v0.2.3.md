# NerazimNet v0.2.3

## Improvements

- **Active-view indicator in the sidebar.** The nav item for the view you're on is now highlighted with a slate accent (derived from the theme's scrollbar color), with light text and icon so it reads cleanly in both light and dark mode. Works collapsed too — the icon-only rail still shows the active view.
